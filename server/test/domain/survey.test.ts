// import { LikertSurvey, LikertItem } from "../../src/domain/survey";

// const survey = new LikertSurvey([new LikertItem("How likely is this test to pass", 5)])

// test('Survey serialises to object', () => {
//     expect(new LikertSurvey([new LikertItem("How likely is this test to pass", 5)])).toBeTruthy()
// })

// test('Survey deserialises', () => {
//     expect(survey.serialise()).toBe(survey)
// })

// test('Survey serialises', () => {
//     const clonedSurvey = new LikertSurvey(survey.items, survey.id)
//     expect(survey).toStrictEqual(clonedSurvey)
// })